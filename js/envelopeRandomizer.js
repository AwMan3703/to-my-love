
const envelope = document.getElementById("envelope")
const flap = envelope.getElementById("flap")
const body = envelope.getElementById("body")
const inside = envelope.getElementById("inside")

const randomize = () => {

}

randomize()
